apiVersion: v1
kind: Service
metadata:
  name: envoy-external-service
  namespace: homepages
spec:
  type: LoadBalancer 
  selector:
    app: envoy 
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 10000